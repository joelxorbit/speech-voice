# Assets Directory
- **logo.png**: Replaced by a responsive CSS text logo (`🎤 VoiceNav`) in the HTML for better performance and scaling.
- **sounds/**: The sound effects (success, error, start listening, navigate) are synthetically generated using the Web Audio API inside `js/speech.js` to ensure they always play without needing external files or worrying about browser caching/loading delays.
